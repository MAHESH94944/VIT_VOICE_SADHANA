import { registerUser, verifyOtp } from "./auth";

// For registration, use registerUser(data)
// For OTP verification, use verifyOtp(email, otp)

// Example usage:
// await registerUser({ ... });
// await verifyOtp(email, otp);
